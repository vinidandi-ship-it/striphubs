export const seoTextForCategory = (category: string) =>
  `Watch live ${category} cam models streaming right now with real-time viewer counts.`;

export const seoTextForTag = (tag: string) =>
  `Browse ${tag} cam performers broadcasting live shows from high-speed rooms.`;

export const seoTextForCombination = (category: string, tag: string) =>
  `Discover live ${category} cam models featuring ${tag} performers for fresh experiences.`;
